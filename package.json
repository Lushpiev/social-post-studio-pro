{
  "name": "telegram-autopost-mobile-22",
  "version": "2.2.0",
  "private": true,
  "scripts": {
    "cap:init": "npx cap sync android",
    "cap:copy": "npx cap copy android",
    "cap:open": "npx cap open android",
    "apk:remote": "node scripts/set-capacitor-url.js && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/cli": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  }
}